# Audit — 2026-07-10 (branch `claude/aurelius-legion-expansion-syxk9f`, PR #2)

Scope: everything on the branch, checked against **NORTH_STAR.md** (which fuses
the OG 3.4 roadmap, CLAUDE.md, and the Command Deck spec). Method: code +
live-fire against local Postgres 16 + pgvector, not memory.

## 1. Build & type health
| Check | Result |
|---|---|
| `npx tsc --noEmit` (backend, unfiltered) | **0 errors** — the §4 "known TS debt" files are fixed |
| `npx next build` (frontend, prod) | **green**, full route manifest incl. /wiki, /engines, manifest.webmanifest |
| Prisma migrations (local replay) | **13 applied**, HNSW index intact (the diff-drops-HNSW trap excised in every migration) |

## 2. Performance (dev server, warm, best-of-3)
| Route | Latency |
|---|---|
| /api/deck (heaviest assembly: plan+tasks+habits+signals+stats+goals+activity) | 21ms |
| /api/today | 21ms |
| /api/corpus · /api/wiki · /api/missions · /api/proposals · /api/rituals | 12–13ms |
| /api/engines | 10ms |
| semanticRecall (embed + pgvector HNSW, cold client) | 530ms first-hit; the SQL itself is HNSW-indexed |

No N+1s observed on hot paths; deck assembly batches via Promise.all. Perf risk
to watch: wiki synthesis + mission LLM steps are seconds-long by nature — they
already run fire-and-forget/background, never blocking a request.

## 3. Definition of Done (NORTH_STAR §2) — line by line
| DoD item | Status |
|---|---|
| "Plan my week around training, deep work, and calls" | **Not yet** — needs Calendar engine (blocked on Google OAuth, §6 block 5). Deck/tasks/projects substrate ready. |
| "What are you working on and why?" | **Partial→Yes**: missions with live step states, planSummary, Aurelius page + /api/missions expose current work + reasoning; initiative proposals state *why*. |
| Runs for days; survives restart | **Yes for state** (all state incl. proposals now in Postgres; schedulers rearm on boot; verified restart-read). Multi-day soak untested — needs the Mini. |
| Every decision visible; debug from cockpit alone | **Partial**: missions/steps, signals, rituals, ingestion runs, engines page are visible. Legacy cockpit widgets still mock-fed; no structured per-request trace log yet. |
| Morning briefing + nightly debrief reach the phone | **Yes** (Telegram push wired; dormant until token). Weekly planning session **not built**. |
| Command Deck: my lane / Aurelius lane / bridge; metrics confront | **Yes** — deck + Today + Bridge + hero counts incl. overdue/attention; follow-through % confronts. |
| Confirmed knowledge changes what Aurelius knows | **Yes** — durable propose→confirm, review bench, Layer 7.5; verified end-to-end. |
| Scoreboard shows LLM-dependence falling | **Partial** — weekly snapshot records patternsActive/reasoningRuns/corrections; an explicit llmDependenceRate metric not yet derived. |

## 4. Sequence coverage (NORTH_STAR §6)
- **0–3 (Layer 7.5, compiled+propose→confirm, retrieval, productivity plane + Today): DONE**, live-verified.
- **4 (continuous learning): DONE** minus Monday-review *dedicated* UX (the Bridge bench covers proposals) and autonomy-escalation matrix.
- **5 (calendar + rituals + push): rituals/push DONE; calendar NOT BUILT** (needs Cole's OAuth) — the one DoD-blocking gap.
- **6 (deck + bridge UI): DONE.** Substrate review screen partially covered by Aurelius page + Bridge.
- **7 (whole-life operators):** infra shared and ready; business/content/finance operators not yet lit (finance plane proposal pending Cole).
- **8 (self-directed research): DONE v1** — initiative pulse + steerable standing topics + sweeps that ingest their own findings.
- **9 (scoreboard + freshness): scoreboard DONE; knowledge freshness/supersession NOT BUILT.**
- **10 (local-first deploy): runbook written** (docs/DEPLOY_MAC_MINI.md); execution needs the hardware.
- **11 (core auto-evolution): NOT BUILT** (by design — later).

## 5. Invariants (§7) — spot-audited in code
- No training prescriptions: mission/ritual/wiki prompts report signals only ✓
- Propose→confirm on Living Knowledge writes: all paths go through proposals or explicit Cole action ✓ (setKnowledge callers: resolveProposal, seed, smoke)
- Provenance: KnowledgeEntry sourceType/sourceId/rationale populated on every write path ✓
- Verified blocks: every block this branch shipped with live-fire verification in its commit message ✓
- Local-first: runbook + VAULT_DIR + PWA + dormant Telegram all align ✓

## 6. Debt register (honest)
1. Legacy cockpit widgets mock-fed (observability lane not load-bearing yet).
2. Two LLM-routing paths still coexist (`core/engineRouter` legacy vs `llm/router`); consolidation deferred deliberately (§5.3).
3. No automated test suite; verification is live-fire smoke scripts. Standing-requirement gap.
4. No structured logging/tracing; console only.
5. Weekly-planning ritual (Sunday session) not implemented.
6. llmDependenceRate not derived on the scoreboard.
7. Knowledge freshness/staleness scoring not built.
8. `scripts/smokeBrain.ts`-style scripts live in scratchpad, not committed as a smoke suite.

## 7. Verdict
The brain, the learning loop, the autonomy ladder (react → schedule → initiate),
the second brain (DB + Obsidian vault), the push, and the measurement plane are
**built and verified**. The one DoD item that cannot be closed without Cole is
the calendar. The riskiest untested claim is multi-day stability — that's the
Mini soak. Recommended next blocks, in order: **calendar engine** (closes the
headline DoD), **smoke-suite + structured logging** (standing requirement),
**cockpit re-wire to real telemetry**, **llmDependenceRate on the scoreboard**.
