# THE BUSINESS SEAT — funnel audit and the road to client #1

**Headline finding: since ROADMAP_DEMAND_ENGINE.md was written, nearly the whole ordered list has shipped.** The emitter (`/aurelius/crm/trackLinks.ts` + `/l/:code` at `/aurelius/index.ts:256`), the `referredBy` fix (`/aurelius/crm/leadEngine.ts:83` — relationship now goes to notes), the Offer Probe (`/aurelius/business/offers.ts:284`), The Standard (`/frontend/app/standard/page.tsx` + `/aurelius/assessment/benchmarks.ts`), Stripe self-record (`/aurelius/crm/selfRecord.ts`), the referral/PR engine (`/aurelius/crm/retention.ts`), the confronting analyst with real-denominator thresholds (`/aurelius/business/analyst.ts:28` — 15 clicks or 3 leads before ranking), paid boost gated on organic proof (`/aurelius/business/paidBoost.ts`), partnership acquisition (`/aurelius/business/partnership.ts`), onboarding runway (`/aurelius/crm/onboarding.ts`). The constitution holds everywhere I checked: publish/send/spend all gate on confirm, `kind: "client"` filters in payment matching and PR business-machinery, empty states are named. **The constraint has moved from missing code to four inputs only Cole can supply.**

## 1. The honest funnel audit

Walking a stranger through what's built: **(a) Attention** — still only Cole's face and reps; the content spine is deliberately dark at `marketingPass.ts:47-61` because no Offer is active (correct behavior, surfaced weekly). **(b) Click→capture** — `/l/:code` counts and 302s to `/start?ref=`; `/start` and `/standard` are real public pages; `captureInboundLead` sanitizes, dedupes, attributes, and pushes to his phone (capped 5/day). **But this whole rung is dark today for a config reason: `APP_PUBLIC_URL`/`MEDIA_PUBLIC_BASE_URL` are unset (COLE_CHECKLIST.md Part B #5-6), so no stranger on Earth can reach any of it.** The system is honest about this (links render as paths with the fix named), but it's the single binding switch. **(c) The Standard's bands are generic defaults** (`benchmarks.ts:33`, permanently `provisional: true`) — Cole never supplied his numbers; the Contrarian's "dishonest scoring" risk is mitigated by labeling, not resolved. Note it correctly excludes sprint times per his own `measurement_constraint` fact. **(d) Capture→sale** — outreach drafts are barred from inventing an offer (`offerContextBlock`'s explicit prohibition) — so until Cole prices and activates one, every conversation ends at "let's talk." **(e) Sale→money** — convertLead, 14-day onboarding runway, signature-verified idempotent Stripe webhook, spoofable payment emails demoted to confirm. **(f) Back-half** — referral/PR/proof correctly dormant. No hand-waving found; the two broken rungs are both Cole-decisions, not code: no public origin, no active offer.

## 2. The warm-list reality

The machinery is sufficient. Paste UI on the Business page (`/frontend/app/(chrome)/business/page.tsx:1900`), dedup on import, every entry gets `nextActionAt: now`, the 07:30 sweep drafts 3/day into Gmail (bounded by his review capacity, not generation), follow-up auto-dated +4 days, reply-matching via `outreachThreadId` flips leads to "conversing." One honest limitation: the sweep is **email-only** — phone-only entries throw rather than fake "contacted" (the phantom-contact bug is fixed at `leadEngine.ts:216`), and a daily gap signal counts them. Since warm relationships mostly live in text, week one realistically looks like: Monday, answer the `first_ten` open question (`profile.ts:225`) and paste 10–20 names with context lines; let the sweep draft for the emailed ones; **Cole texts the phone-only ones himself** and marks them; run `probeOffer` so each of 2–3 offer variants gets its tracked link to float in those conversations; day 4-5, follow-ups surface automatically. Zero new build required.

## 3. Missing-that-matters vs premature

**Matters:** (a) The four Cole inputs — public origin, warm-list paste, offer price/activation, real benchmark bands. (b) **The one-off program product his own `billing_shapes` fact already names**: a "program" Offer + a Stripe payment link is the lowest-friction yes a warm list can give at zero trust, and delivery is the Google Sheet he already uses — no platform build needed. (c) A one-tap "I texted them" action on lead cards, so the phone-lane doesn't depend on Cole remembering to log. (d) A config surface for benchmark bands (today a code table). **Premature and correctly parked:** paid boost (self-gates: needs 3+ measured posts), partnership sweep (needs a research key; produces partners, not leads), SMS 10DLC, the athlete portal (`delivery_scope` is still an open question), all scale machinery. On proof artifacts: the gym's measured numbers are the **local** proof model per Cole's own `online_proof_model` fact — the remote business genuinely has no proof until client #1, and the dormant proof engine is the honest answer, not a gap.

## 4. The 30-day plan to client #1

- **Days 1–3:** Set `APP_PUBLIC_URL` + `MEDIA_PUBLIC_BASE_URL` (deploy runbooks exist). Paste the warm list. Run the Offer Probe — three variants, three tracked links.
- **Week 1–2:** Sweep drafts 3/day; Cole sends emails, texts the rest by hand. Float probe links in those conversations and one IG story. Supply real benchmark bands; set the bio link to a minted `/l/` code pointing at `/standard`. He can post his dichotomy reels manually with the tracked link — the dark content spine doesn't block him.
- **Week 3:** Price and activate the leading variant (`activateOffer` refuses unpriced — correctly). The Sunday 16:00 marketing pass lights up on its own; outreach now quotes a real thing. If the "program" shape leads, attach a Stripe payment link.
- **Week 4:** First yes → `convertLead` fires the onboarding runway; set `STRIPE_WEBHOOK_SECRET` (or use the Venmo confirm path); client #1 arms the referral flywheel that's already waiting.

**New builds this plan actually justifies — three, all small:** program-sale fulfillment (payment link on an active "program" offer + a "send the sheet" onboarding task), the "I texted them" tap, and the benchmark-bands config surface. Everything else on the roadmap is built, gated, and honest. The Adversary's closing line still governs: amplification of zero is zero — the machine is ready, and the remaining inputs are Cole's hands only.
